# IIoT constraints

IIoT systems encompass a variety of platforms from small devices to enterprise machines, with an even greater variety in terms of environments, memory footprints, CPUs, and programming language across the components and systems, which must be supported by the connectivity framework. Each platform has its own unique constraints for memory, processing, and storage capacity.
