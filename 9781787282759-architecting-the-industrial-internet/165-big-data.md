# Big data

Relational databases generally expect data to conform to defined structures; therefore, unstructured and semi-structured data must be transformed to a predefined schema structure in a relational database or stored as a blob object. High-velocity and high-volume data present additional challenges in mapping each record or packet to its proper place in the database within a short-term time horizon. Queries are also difficult to perform without the benefit of structure and data normalization.
